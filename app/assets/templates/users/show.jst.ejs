<div class="clearfix">
  <div class="pull-left">
    <%= view.imageTag(user.get('profilePicShowUrl'),
                      { className: "img-circle" }) %>
  </div>
  <h1><%= user.get('username') %></h1>
  
  <div id="follow">
    <% if (user.isFollowedBy(currentUser)) { %>
      <button class="unfollow btn btn-default btn-sm">unfollow</button>
    <% } else if (user.id != currentUser.id) { %>
      <button class="follow btn btn-default btn-sm">follow</button>
    <% } %>
  </div>
</div>
  
<% if (user.id == currentUser.id) { %>
  <%= view.linkTo(
            "change profile photo",
            "#",
            { id: "change-profile-pic" }) %>
            
  <div id="profile-pic">
  </div>  
<% } %>

<div class="btn-group" data-toggle="buttons">
  <label class="btn btn-default" id="grid">
    <input type="radio">
    <span class="glyphicon glyphicon-th-large"></span>
  </label>
  <label class="btn btn-default" id="vertical">
    <input type="radio">
    <span class="glyphicon glyphicon-align-justify"></span>
  </label>
</div>

<div id="photos">
</div>
